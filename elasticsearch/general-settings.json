{
  "version": 1,
  "template": {
    "settings": {
      "index": {
        "codec": "best_compression",
        "mapping": {
          "total_fields": {
            "limit": "10000"
          }
        },
        "number_of_shards": "1",
        "number_of_replicas": "1"
      }
    }
  },
  "_meta": {
    "description": "This component template contains general settings for all indices."
  }
}
